<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-feature android:name="android.hardware.camera" android:required="true" />
    <uses-feature android:name="android.hardware.camera.autofocus" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.gyroscope" android:required="false" />
    <uses-feature android:name="android.hardware.sensor.accelerometer" android:required="false" />

    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"
        android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"
        android:maxSdkVersion="28" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES"
        android:minSdkVersion="33" />
    <!-- 幻影模式权限 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <!-- 查询微信/微博包以支持分享 -->
    <queries>
        <package android:name="com.tencent.mm" />
        <package android:name="com.sina.weibo" />
    </queries>

    <application
        android:name=".LiveCaptureApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="构妙 LiveCapture"
        android:supportsRtl="true"
        android:theme="@style/Theme.LiveCapture">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden"
            android:windowSoftInputMode="adjustResize"
            android:theme="@style/Theme.LiveCapture">

            <!-- 主启动入口 -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- 快捷拍摄入口 -->
            <intent-filter>
                <action android:name="com.livecompose.livecapture.QUICK_CAPTURE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- 系统相机快捷入口 -->
            <intent-filter>
                <action android:name="android.media.action.STILL_IMAGE_CAMERA" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>

            <!-- 锁屏快捷入口 -->
            <intent-filter>
                <action android:name="android.media.action.STILL_IMAGE_CAMERA_SECURE" />
                <category android:name="android.intent.category.DEFAULT" />
            </intent-filter>
        </activity>

        <!-- 快捷方式 -->
        <shortcut
            android:shortcutId="quick_capture"
            android:enabled="true"
            android:icon="@mipmap/ic_launcher"
            android:shortcutShortLabel="快速抓拍"
            android:shortcutLongLabel="快速抓拍模式">
            <intent
                android:action="com.livecompose.livecapture.QUICK_CAPTURE"
                android:targetPackage="com.livecompose.livecapture"
                android:targetClass="com.livecompose.livecapture.MainActivity" />
        </shortcut>

        <!-- 幻影模式前台服务 -->
        <service
            android:name=".core.phantom.PhantomService"
            android:exported="false"
            android:foregroundServiceType="dataSync" />

        <!-- 幻影模式快速设置磁贴 -->
        <service
            android:name=".core.phantom.PhantomTileService"
            android:exported="true"
            android:icon="@mipmap/ic_launcher"
            android:label="幻影模式"
            android:permission="android.permission.BIND_QUICK_SETTINGS_TILE">
            <intent-filter>
                <action android:name="android.service.quicksettings.action.QS_TILE" />
            </intent-filter>
        </service>

        <!-- FileProvider 用于分享图片 -->
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- 微信分享回调 Activity（包名路径 wxapi.WXEntryActivity 为微信SDK强制要求） -->
        <activity
            android:name=".wxapi.WXEntryActivity"
            android:exported="true"
            android:theme="@android:style/Theme.Translucent.NoTitleBar"
            android:launchMode="singleTop" />

        <!-- 合规页面（隐私政策、用户协议、SDK清单） -->
        <activity
            android:name=".features.compliance.ComplianceHostActivity"
            android:exported="false"
            android:theme="@style/Theme.LiveCapture" />
    </application>
</manifest>
